# Groundtrack — runtime and tooling dependencies
# CPU only. No GPU, no Docker, no paid services.

# Data + metric
numpy>=1.26
pandas>=2.2
pyarrow>=15.0          # reads the parquet telemetry benchmark
scikit-learn>=1.4

# Reporting
matplotlib>=3.8         # renders results/progress.png from results/ledger.jsonl

# IBM Bob drives the forge loop through its own CLI (`bob run`), installed
# separately via npm as `bobshell`. It is intentionally NOT a Python dependency —
# Bob is a development-time tool and is never imported at runtime.

# IBM Granite runs locally through Ollama for offline brief generation.
# Install Ollama, then: ollama pull granite4:3b
